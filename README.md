# Passme

passme - A terminal local encrypted password manager cli tool.

### install

$ `./install.sh`

Note: Currently its only tested on MacOs

### usage

$ `sudo passme -h`

### unistall

$ `./unistall.sh`

